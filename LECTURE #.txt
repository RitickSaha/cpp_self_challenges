1)CHAR()

2)ASCII()

3)INITCAP()
	SELECT ENAME INITCAP(ENAME) FROM EMP;
4)CONCAT()
	SELECT ENAME CONCAT ('Mr. ',ENAME) from EMP;
5)SUBSTR()
	SELECT ENAME SUBSTR(ENAME,1,3) FROM EMP;
6)LPAD()
	SELECT ENAME,SAL,LPAD(TO_CHAR(SAL),15,'*')FROM EMP;
7)DECODE()
	SELECT ENAME,JOB,DECODE(JOB,'MANAGER','RM','CLERK','EX',JOB) FROM EMP;
8)SWITCH CASE
	SELECT ENAME , JOB CASE JOB WHEN 'MANAGER' THEN 'RM' WHEN 'CLERK' THEN 'EX' ELSE JOB END FROM EMP
9)REPLACE
	SELECT ENMAE REPLACE(ENAME,'SM','AM') FROM EMP;
	